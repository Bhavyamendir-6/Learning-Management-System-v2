# ── Core AI / Agent ──────────────────────────────────────────────────────────
google-adk                # Google Agent Development Kit
google-generativeai       # Gemini SDK (genai.Client, types, File Search)
python-dotenv             # .env loading

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.0             # BaseModel, Field, field_validator (v2 API)

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0           # Async ORM (application tables + ADK DatabaseSessionService)
asyncpg                   # PostgreSQL async driver (postgresql+asyncpg:// URL)

# ── Backend Framework (FastAPI) ───────────────────────────────────────────────
fastapi
uvicorn
sse-starlette             # For Server-Sent Events (SSE) streaming
python-multipart          # For handling file uploads (multipart form data)

# ── Authentication ────────────────────────────────────────────────────────────
PyJWT>=2.8.0              # JWT signing/verification (HS256)
bcrypt>=4.0.0             # Password hashing (work factor 12)

# ── Dependency Resolution Bounds (Render Fix) ─────────────────────────────────
# Locking down ALL dependencies that caused pip to loop
google-api-python-client==2.187.0
googleapis-common-protos==1.72.0
grpcio==1.76.0
idna==3.11
charset-normalizer==3.4.4
requests==2.32.5
PyYAML==6.0.3
pyparsing==3.3.1
urllib3==2.6.3
uritemplate==4.2.0
tzlocal==5.3.1
typing-extensions==4.15.0
tenacity==9.1.2
starlette==0.50.0
fastapi==0.123.10
pydantic==2.12.5
uvicorn==0.40.0
